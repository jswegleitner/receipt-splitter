{
  "name": "receipt-splitter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Receipt Splitter — single-file bill-splitting web app. Source lives in src/; `npm run build` emits the deployed index.html.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "tailwindcss": "^3.4.19"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
